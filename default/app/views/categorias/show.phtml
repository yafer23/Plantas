<a href="<?= URL_APP ?>categorias/" class="btn btn-outline-success ">Regresar</a>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-8 col-md-10 col-sm-12">
            <div class="card shadow rounded-4 border-0">
                <div class="card-header bg-primary text-white azul-header">
                    <h4 class="mb-0">
                        Categoría #<?= $categoria->id ?> - <?= $categoria->nombre ?>
                    </h4>
                </div>
                <div class="card-body">
                    <p class="mb-2">
                        <strong>ID:</strong> <?= $categoria->id ?>
                    </p>
                    <p class="mb-3">
                        <strong>Nombre:</strong> <?= $categoria->nombre ?>
                    </p>

                    <?php
                    $productos = $categoria->getProductos();
                    $cantidad_productos = is_array($productos) ? count($productos) : 0;
                    ?>

                    <p class="mb-4">
                        <strong>Productos en esta categoría:</strong>
                        <span class="badge bg-info text-dark">
                            <?= $cantidad_productos ?>
                        </span>
                    </p>

                    <?php if ($cantidad_productos): ?>
                        <ul class="list-group mt-3">
                            <?php foreach ($productos as $producto): ?>
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    <span class="fw-medium"><?= $producto->nombre ?></span>
                                    <span class="badge bg-success fs-5">
                                        $<?= number_format($producto->precio, 2) ?>
                                    </span>
                                </li>
                            <?php endforeach; ?>
                        </ul>
                    <?php else: ?>
                        <div class="alert alert-warning mt-3">
                            Esta categoría no tiene productos aún.
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .card.shadow {
        box-shadow: 0 0 30px rgba(0, 123, 255, 0.1);
        padding: 2rem;
        border: none;
        border-radius: 0.75rem;
    }

    .card-header.azul-header {
        border-radius: 0.75rem 0.75rem 0 0;
        font-weight: bold;
        font-size: 1.25rem;
    }

    .list-group-item {
        font-size: 1.1rem;
    }

    .badge.bg-success.fs-5 {
        padding: 0.6rem 1rem;
        font-size: 1.2rem;
    }
</style>
