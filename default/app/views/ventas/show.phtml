<a href="<?= URL_APP ?>ventas/" class="btn btn-outline-success">Regresar</a>

<div class="card shadow p-4 border-2 border-primary" style="max-width: 600px; margin: auto; font-family: 'Courier New', monospace;">
    <h4 class="text-center mb-3 text-muted">🧾 Ticket de Venta</h4>

    <div class="mb-2">
        <span class="text-muted">ID Venta:</span>
        <span class="text-primary fw-bold"><?= $venta->id ?></span>
    </div>

    <div class="mb-2">
        <span class="text-muted">Cliente:</span>
        <span class="text-primary fw-bold"><?= $venta->cliente_nombre ?? 'Desconocido' ?> (ID: <?= $venta->clientes_id ?>)</span>
    </div>

    <div class="mb-2">
        <span class="text-muted">Método de Pago:</span>
        <span class="text-primary fw-bold"><?= $venta->metodo_pago_nombre ?? 'Método no registrado' ?></span>
    </div>

    <div class="mb-2">
        <span class="text-muted">Fecha:</span>
        <span class="text-primary fw-bold"><?= $venta->updated_in ?? $venta->fecha ?></span>
    </div>

    <div class="mb-2">
        <span class="text-muted">Total de la Venta:</span>
        <span class="text-primary fw-bold">$<?= number_format($venta->total, 2) ?></span>
    </div>

    <div class="border-top border-primary my-3"></div>

    <p class="text-center text-muted fw-semibold" style="font-size: 0.9em;">Venta registrada. Verifique que los datos sean correctos.</p>
</div>
